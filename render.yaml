services:
  - type: web
    name: py-runner-bot
    env: python
    plan: free

    pythonVersion: 3.11.9   # 🔥 MUTLAKA

    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py

    envVars:
      - key: BOT_TOKEN
        sync: false
